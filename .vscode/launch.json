{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "lldb",
			"request": "attach",
			"name": "Attach",
			"preLaunchTask": "rust: cargo xtask run",
			"program": "${env:EXANIMA_EXE}",
		}
	]
}
